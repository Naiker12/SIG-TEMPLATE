# Reemplaza la URL con la cadena de conexión de tu proyecto de Supabase.
# La encuentras en: Supabase > Configuración del Proyecto > Base de Datos > Cadena de conexión > URI
DATABASE_URL="postgresql://postgres:[TU_CONTRASENA]@[ID_PROYECTO].supabase.co:5432/postgres"

# Clave secreta para firmar los tokens JWT.
# Puedes generar una segura con: openssl rand -hex 32
SECRET_KEY="tu_clave_secreta_aqui"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
