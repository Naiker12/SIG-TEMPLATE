# === Variables de Entorno del Backend ===
# Este archivo es para tus secretos y no debe ser subido a Git.

# --- Base de Datos (Supabase + Prisma) ---
# Reemplaza [TU_CONTRASEÑA] con tu contraseña real de la base de datos de Supabase.
# Se ha configurado para usar el Connection Pooler (puerto 6543) como recomienda Supabase para producción.
# El parámetro `connection_limit=1` es una optimización recomendada por Prisma para Supabase.
DATABASE_URL="postgresql://postgres:1234567890Naiker$@db.eadqpevjvguzizsbrujk.supabase.co:6543/postgres?pgbouncer=true&connection_limit=1"

# --- Autenticación JWT ---
# Clave secreta para firmar los tokens. Puedes generar una con: openssl rand -hex 32
SECRET_KEY="TU_CLAVE_SECRETA_AQUI_DEBE_SER_LARGA_Y_SEGURA"

# --- Supabase Storage (Opcional, para subida de archivos) ---
# Reemplaza con la URL y la clave anónima de tu proyecto de Supabase.
# Las puedes encontrar en "Project Settings" > "API" en tu dashboard de Supabase.
SUPABASE_URL="https://eadqpevjvguzizsbrujk.supabase.co"
SUPABASE_ANON_KEY="TU_SUPABASE_ANON_KEY"
